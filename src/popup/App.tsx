import { Button, Container } from "@mantine/core";
import { useState } from "react";

function App() {
  const [count, setCount] = useState(0);

  return (
    <Container>
      <Button>wef</Button>
    </Container>
  );
}

export default App;
